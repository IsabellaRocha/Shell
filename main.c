#include "headers.h"

int main(int argc, char *argv[]) {
    boolean running = true;
    boolean input = false;
    printf("$ ");
    while (running) {
        char** args;
        if(sizeof(args) && input) {
            printf("$ ");
            input = false;
        }

    }
}
